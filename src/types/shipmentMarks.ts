const enum ShipmentMarks {
    FRAGILE = 'Fragile',
    DO_NOT_LEAVE = 'Do not leave if address not at home',
    RETURN_RECEIPT_REQUESTED = 'Return receipt requested',
}

export {
    ShipmentMarks
};
